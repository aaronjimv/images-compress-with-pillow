# Images compress with pillow
---

This repository contains a simple code to compress images with PIL (pillow).

Only you will need:
> `pip install pillow`

Put the images to compress in `/to-compress` folder.
